import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    gridContainer: {
        
    },
    appBarSearch: {
        alignItems:'center',
        borderRadius:'10px',
        padding:'10px',
    },
    searchButton: {
        margin:'5px'
    }
}));